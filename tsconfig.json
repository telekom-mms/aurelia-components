{
  "compilerOptions": {
    "target": "es2022",
    "module": "esnext",
    "esModuleInterop": true,
    "stripInternal": true,
    "preserveConstEnums": true,
    "skipLibCheck": true,
    "declaration": true,
    // TypeScript 6 no longer auto-includes node_modules/@types; anything global
    // has to be requested here. "src" itself needs no global types -- "jest" is
    // kept so a test-side type check can extend this config. A test check also
    // needs "node" (for `global`), which requires adding @types/node first.
    "types": [
      "jest"
    ],
    // TypeScript 6 turns "strict" on by default. The explicit strict* flags
    // below stay in effect; this only keeps the pre-6 baseline for the rest.
    "strict": false,
    "removeComments": true,
    "allowSyntheticDefaultImports": true,
    "noPropertyAccessFromIndexSignature":  true,
    "strictBindCallApply":  true,
    "strictFunctionTypes": true,
    "useUnknownInCatchVariables": true,
    "importHelpers": true,
    "sourceMap": true,
    "lib": [
      "es2020",
      "es2021.intl",
      "dom"
    ],
    "rootDir": "src",
    "outDir": "dist"
  },
  "include": [
    "src"
  ],
  "exclude": [
    "node_modules",
    "dist"
  ]
}
